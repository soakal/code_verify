#!/usr/bin/env python3
"""
encrypt_key.py — No longer needed.

codex-verify now uses the Claude Code CLI (claude -p), which authenticates
via your Claude Code subscription. No API key is required.

This file is kept for reference only and does nothing useful.
"""

import sys

print("Note: encrypt_key.py is no longer needed.")
print("codex-verify authenticates via the Claude Code CLI (claude -p).")
print("Just run: /codex-verify myfile.py")
sys.exit(0)
